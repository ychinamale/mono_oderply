{
  "extends": "npm-package-json-lint-config-default",
  "rules": {
    "prefer-absolute-version-dependencies": ["error", {"exceptions": ["@oderply/shared"]}],
    "prefer-absolute-version-devDependencies": "error"
  }
}
