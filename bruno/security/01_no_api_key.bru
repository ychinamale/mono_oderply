meta {
  name: Security - missing x-api-key
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/v1/panics
  body: json
  auth: none
}

body:json {
  {
    "externalUserId": "x",
    "latitude": 0,
    "longitude": 0,
    "idempotencyKey": "{{$randomUUID}}"
  }
}

assert {
  res.status: eq 401
}
