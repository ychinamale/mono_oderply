meta {
  name: Login - wrong password
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: none
}

body:json {
  {
    "email": "{{operatorEmail}}",
    "password": "wrongpassword"
  }
}

assert {
  res.status: eq 401
}
