meta {
  name: List partners - all
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/partners
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.data: isDefined
}
