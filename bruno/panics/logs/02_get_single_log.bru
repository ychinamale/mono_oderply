meta {
  name: Get single audit log
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/api/v1/panics/{{panicId}}/logs/{{logId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{logId}}
}
