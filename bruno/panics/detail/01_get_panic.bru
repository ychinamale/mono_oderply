meta {
  name: Get panic - success
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/api/v1/panics/{{panicId}}
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.id: eq {{panicId}}
  res.body.partner.apiKeyHash: isUndefined
}
