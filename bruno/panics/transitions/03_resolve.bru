meta {
  name: Resolve panic
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/panics/{{panicId}}/resolve
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.status: eq RESOLVED
}
