meta {
  name: Dispatch panic
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/panics/{{panicId}}/dispatch
  body: none
  auth: none
}

headers {
  Authorization: Bearer {{token}}
}

assert {
  res.status: eq 200
  res.body.status: eq DISPATCHED
}
